{
  # Override the profile for this machine
  profile = "framework"; # Change to "wsl", "work", "vm", etc.

  # Optional: Override other settings as needed per machine
  # hostname = "my-machine";
  # username = "different-user";  # e.g., set to "danny.sok" for macOS machines
}

